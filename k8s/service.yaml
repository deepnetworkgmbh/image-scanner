apiVersion: v1
kind: Service
metadata:
  name: kube-scanner-webapp
  labels:
    app: kube-scanner-webapp
    module: kube-scanner
spec:
  selector:
    app: kube-scanner-webapp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
